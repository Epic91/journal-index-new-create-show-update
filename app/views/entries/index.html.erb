<h1>All your Journal entries!</h1>

<% @entries.each do |entry| %>
    <div>
        <%= link_to entry.name, entry_path(entry)%> | <%= entry.subject %>
    </div>
<% end %>
<br>
<%= link_to 'Add a new entry', new_entry_path%>
